version: "3.7"

services:
  caddy:
    image: caddy:latest
    container_name: caddy
    restart: unless-stopped
    user: 993:989
    environment:
      - PUID=993
      - PGID=989
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - /home/closure/docker/caddy/Caddyfile:/etc/caddy/Caddyfile
      - /home/closure/docker/caddy/data:/data
      - /home/closure/docker/caddy/config:/config

volumes:
  caddy_data:
    external: true
  caddy_config:

networks:
  default:
    name: caddy_net
    external: true
